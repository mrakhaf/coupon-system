# Docker Environment Configuration
# This file contains environment variables for the Docker setup

# Database Configuration (for local development)
DB_HOST=db
DB_PORT=3306
DB_USER=coupon_user
DB_PASSWORD=coupon_password
DB_NAME=coupon_system

# Application Configuration
PORT=8080

# MySQL Configuration (for the db service)
MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD:-root_password}
MYSQL_DATABASE=coupon_system
MYSQL_USER=coupon_user
MYSQL_PASSWORD=${MYSQL_PASSWORD:-coupon_password}
